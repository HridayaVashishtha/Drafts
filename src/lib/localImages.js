// localImages loader deprecated — project now uses cloud-hosted images specified in the sheet.
// keep a minimal stub so imports don't crash if referenced elsewhere.
export function loadLocalImages(){
  return { files: {}, sakshi: [], spotify: [] }
}
